// sections
import SocialAdminView from 'src/sections/social-admin/view';

// ----------------------------------------------------------------------

export const metadata = {
  title: 'Dashboard: Jobs / Đồng bộ',
};

export default function JobsPage() {
  return <SocialAdminView module="jobs" />;
}
